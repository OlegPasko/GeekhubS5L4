<%= form_for(@post) do |f| %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

Post user: <%= @post.user.name unless @post.new_record? %>
<br/>

<%= link_to new_post_path do %>
    <strong>Новый пост</strong>
<% end %>