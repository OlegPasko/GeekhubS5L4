<%= hello(current_user.name) if current_user %>

<p id="notice"><%= notice %></p>

<div class="search-form">
  <%= form_tag('posts', method: :get) do %>
    <%= search_field_tag :query, params[:query] %>
    <%= submit_tag 'Найти!' %>
  <% end %>
</div>
<h1>Listing Posts</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Body</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.title %></td>
        <td><%= post.body %></td>
        <td><%= post.user.name %></td>
        <td><%= link_to 'Show', post, class: "ololo #{'disabled' if current_user}" %></td>
        <td><%= link_to 'Edit', edit_post_path(post) %></td>
        <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to image_tag('itunes.png'), new_post_path %>
<%= image_tag('itunes.png') %>
